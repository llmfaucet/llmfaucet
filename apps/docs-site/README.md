# Documentation site boundary

The active documentation site is `apps/web`, where the existing Next.js App
Router and Fumadocs implementation live. This directory is reserved for a
future dedicated docs build only if the product outgrows the unified web app.
