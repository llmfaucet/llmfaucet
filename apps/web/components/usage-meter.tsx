export function UsageMeter({ used, limit }: { used: number; limit: number }) { const percent = limit ? Math.min(100, Math.round((used / limit) * 100)) : 0; return <div><div className="flex justify-between text-sm"><span>{used.toLocaleString()} used</span><span className="text-muted-foreground">{limit.toLocaleString()} limit</span></div><div className="mt-3 h-2 overflow-hidden rounded-full bg-muted"><div className="h-full rounded-full bg-emerald-500 transition-[width] duration-300" style={{ width: `${percent}%` }} /></div><p className="mt-2 text-xs text-muted-foreground">{percent}% of today&apos;s requests</p></div>; }

