import Link from 'next/link';
import { ArrowUpRight, LockKeyhole, Scale, ShieldCheck, Waypoints } from 'lucide-react';
import { SiGithub } from 'react-icons/si';
export function WhyLlmfaucet() { const cards = [['One contract, many routes', 'One familiar request format instead of a different integration for every source.', '/docs/quickstart'], ['Honest about capacity', 'Free capacity changes; route health, limits, and degraded conditions stay visible.', '/docs/limits'], ['Agent-ready by default', 'Use the same endpoint with agents, SDKs, scripts, and prototypes.', '/docs/agents'], ['Privacy-minded', 'Rotating anonymous identifiers, hashed API keys, and discarded OAuth tokens.', '/docs/privacy'], ['Open development', 'Follow the roadmap and submit reproducible compatibility issues.', 'https://github.com/llmfaucet/llmfaucet'], ['Fair access', 'Budgets and queue priority protect shared public capacity from abuse.', '/docs/limits']]; return <section className="border-y border-border bg-muted/20"><div className="mx-auto max-w-7xl px-5 py-20 lg:px-8"><p className="font-mono text-xs uppercase tracking-widest text-emerald-600">Product principles</p><h2 className="mt-3 text-3xl font-semibold tracking-tight">Built for real developer workflows.</h2><p className="mt-4 max-w-2xl leading-7 text-muted-foreground">A simple public endpoint, clear limits, and an honest path from experimentation to reliable infrastructure.</p><div className="mt-10 grid gap-3 sm:grid-cols-2 lg:grid-cols-3">{cards.map(([title, body, href], index) => { const Icon = [Waypoints, Scale, ArrowUpRight, LockKeyhole, SiGithub, ShieldCheck][index]; return <Link href={href} key={title} className="rounded-2xl border border-border bg-background p-6 transition-colors hover:border-emerald-500/50"><Icon className="size-5 text-emerald-500" aria-hidden="true" /><h3 className="mt-5 font-semibold">{title}</h3><p className="mt-2 text-sm leading-6 text-muted-foreground">{body}</p></Link>; })}</div></div></section>; }
