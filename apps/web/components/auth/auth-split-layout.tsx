import type { ReactNode } from 'react';
import { Code2, KeyRound, Route, ShieldCheck } from 'lucide-react';
import { WaveGridBackground } from '@/components/ui/wave-grid-background';
export function AuthSplitLayout({ children }: { children: ReactNode }) { return <div className="grid min-h-screen lg:grid-cols-2"><div className="flex flex-col p-6 sm:p-10">{children}</div><div className="relative hidden overflow-hidden bg-[#111315] text-white lg:block"><WaveGridBackground className="absolute inset-0 opacity-20" autoAnimate={false} vignette={false} aria-hidden="true" /><div className="relative flex h-full items-end p-12"><div className="max-w-lg"><p className="font-mono text-sm text-emerald-400">LLMFAUCET</p><h2 className="mt-5 text-4xl font-semibold leading-tight">One endpoint for free AI models.</h2><p className="mt-5 leading-7 text-zinc-300">Connect coding tools and OpenAI-compatible apps to public capacity with clear limits, fair routing, and real status signals.</p><div className="mt-8 grid gap-3 text-sm text-zinc-300 sm:grid-cols-2">{[['OpenAI-compatible', Code2], ['GitHub sign-in', KeyRound], ['Personal API keys', Route], ['Fair-use limits', ShieldCheck]].map(([label, Icon]) => <span key={label as string} className="inline-flex items-center gap-2"><Icon className="size-4 text-emerald-400" />{label as string}</span>)}</div></div></div></div></div>; }
