export function SectionHeading({ eyebrow, title, body }: { eyebrow?: string; title: string; body?: string }) { return <div className="max-w-2xl">{eyebrow && <p className="font-mono text-xs uppercase tracking-[0.18em] text-emerald-600 dark:text-emerald-400">{eyebrow}</p>}<h2 className="mt-3 text-3xl font-semibold tracking-tight sm:text-4xl">{title}</h2>{body && <p className="mt-4 text-base leading-7 text-muted-foreground">{body}</p>}</div>; }
