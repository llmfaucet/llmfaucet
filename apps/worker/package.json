{
  "name": "@llmfaucet/worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "lint": "tsc --noEmit",
    "test": "tsx test/gateway.test.ts && tsx test/waitlist.test.ts && tsx test/product-components.test.ts && tsx test/public-metrics.test.ts",
    "deploy:preview": "wrangler deploy --env preview",
    "deploy:production": "wrangler deploy --env production"
  },
  "dependencies": {
    "@llmfaucet/config": "file:../../packages/config",
    "@llmfaucet/types": "file:../../packages/types"
  },
  "devDependencies": {
    "wrangler": "4.86.0"
  }
}
