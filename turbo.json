{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": { "dependsOn": ["^build"], "outputs": [".next/**", "out/**", "dist/**"] },
    "test": { "outputs": [] },
    "lint": { "outputs": [] },
    "typecheck": { "outputs": [] }
  }
}
