# Contributing

Keep browser code in `apps/web`, Worker/runtime code in `apps/worker`, and
scheduled probe code in `apps/worker/src/probe.ts`. Do not import `internal/third-party` into runtime
packages. Run the root verification script before submitting changes.
