# UI guidelines

Use the existing Next App Router in `apps/web`, shadcn components, visible
focus states, reduced-motion support, and real API data. Never add fake counts,
provider guarantees, or fake activity to marketing or dashboard screens.
