# GitHub Sponsors

Sponsors are an entitlement layer, not an upstream credential pool. Active
tiers map to request limits and queue priority; they do not promise models,
tokens, availability, or an SLA.
