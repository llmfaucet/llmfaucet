# Resource inventory

The authoritative binding definitions are in `apps/worker/wrangler.toml`.
Keep preview IDs and production IDs separate; record operator-owned resource
IDs in deployment secrets or the private infrastructure inventory.
