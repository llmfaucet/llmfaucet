# Anthropic compatibility

`/v1/messages` translates the supported Anthropic request/response surface to
the Worker’s normalized chat contract. Provider capabilities still determine
whether a request can be routed.
